# Copy this file to .env (gitignored, never commit real credentials) and
# fill in real values. docker-compose automatically loads .env from this
# same directory — no extra setup needed locally or when this gets synced
# to the NAS by scripts/deploy-docker.sh (which does not exclude .env from
# the transfer, matching how appsettings.json is already handled).

# ─── Host paths ───────────────────────────────────────────────────────────

# Root of the media library on the NAS — the parent of Movies, TV, audiobooks,
# PhotoPrints and so on. Every media mount in docker-compose.yml is built from
# this, so it is the one place to change if the array ever moves.
#
# It lives here rather than in docker-compose.yml because the real value is a
# path under a home directory, i.e. a username, and docker-compose.yml is
# committed. Set the real path in .env, which is gitignored.
MEDIA_ROOT=/home/youruser/Media

# PIA account credentials — NOT your PIA "site" login, use the actual VPN
# username (looks like pXXXXXXX) and password from your PIA account page.
PIA_USER=
PIA_PASSWORD=

# Optional. Comma-separated PIA region name(s), e.g. "US East". Leave blank
# to let Gluetun pick automatically. See:
# https://github.com/qdm12/gluetun-wiki/blob/main/setup/servers.md
PIA_SERVER_REGIONS=

# Optional. Same PIA account, independent region choice for the torrent VPN
# gateway (gluetun-torrent) used by qBittorrent/Sonarr/Radarr. Leave blank
# to let Gluetun pick automatically.
PIA_TORRENT_SERVER_REGIONS=

# Sonarr/Radarr API keys — these are NOT chosen up front. Each app generates
# its own key on first boot (visible in its web UI under Settings General);
# deploy once, grab both keys from there, then fill these in and redeploy.
SONARR_API_KEY=
RADARR_API_KEY=

# Jellyfin API key — Dashboard -> Advanced -> API Keys -> create one. Used
# server-to-server to match Sonarr/Radarr's downloaded content to Jellyfin's
# own library items, for the embedded "Watch Now" player.
JELLYFIN_API_KEY=

# Paul/Mom/Dad's own Jellyfin logins (Dashboard -> Users) — enables real
# per-person playback (their own Jellyfin permissions) and resume-position
# tracking that survives logging out of this app. Leave blank to keep the
# old shared embedded-player behavior for that person (no resume). If you
# change a password in Jellyfin, just update it here and redeploy. This can
# be the same admin account JELLYFIN_API_KEY above belongs to, or a separate
# one — either works the same way.
JELLYFIN_PAUL_USERNAME=
JELLYFIN_PAUL_PASSWORD=
JELLYFIN_MOM_USERNAME=
JELLYFIN_MOM_PASSWORD=
JELLYFIN_DAD_USERNAME=
JELLYFIN_DAD_PASSWORD=

# Audiobookshelf API key — Settings -> Users -> API Keys -> create one
# (deploy Audiobookshelf first, complete its admin setup, add the
# /audiobooks library, then generate this). Used server-to-server to fetch
# the audiobook catalog and stream audio.
AUDIOBOOKSHELF_API_KEY=

# Listenarr API key — generated automatically on first run. Read it from
# Listenarr's API Key settings (or its localhost-only bootstrap endpoint before
# login is enabled) and place it here. Used only by the server-side audiobook
# request proxy; never exposed to the Angular app.
LISTENARR_API_KEY=

# Google Books API key — optional but recommended (Google Cloud Console,
# free tier, no billing needed: enable the "Books API" and create an API
# key). Used by AudiobookEnrichmentService to identify messy-named
# audiobooks; anonymous requests work but are rate-limited/unreliable at
# the volume this service needs for an initial backlog pass.
GOOGLE_BOOKS_API_KEY=

# Your Tailscale MagicDNS hostname for this NAS (e.g.
# ugreen-nas.tailXXXXX.ts.net — find it via `tailscale status` or the
# Tailscale admin console). Used to build the Jellyfin embed URL and to
# scope the CSP header that allows the embedded player's iframe — kept out
# of committed files since it's specific to your own private tailnet.
TAILSCALE_HOSTNAME=

# One-time Plex claim token from https://plex.tv/claim (valid 4 minutes) —
# only needed the first time the plex container starts, to associate it with
# your Plex.tv account. Safe to blank out again afterward.
PLEX_CLAIM=

# LAN address of the gaming PC that the wake/sleep control targets. Only used
# when Gaming__Enabled=true in docker-compose.yml; the status endpoint reports
# "not configured" while it's blank. Kept here rather than in tracked source so
# no LAN address is ever committed.
GAMING_PC_IP=

# ─── Immich (household photo library) ────────────────────────────────────
# Postgres password for Immich's own database. Generate any long random
# string — it is never typed by a human, only read by the two containers:
#   openssl rand -base64 32
# Immich's database is entirely separate from this app's SQLite state; losing
# this password means recreating the Immich DB, which loses albums, faces and
# sharing (the original photo files under IMMICH_UPLOAD_LOCATION survive).
IMMICH_DB_PASSWORD=

# Where Immich stores originals, thumbnails and its own metadata. Must be on
# the HDD array, not the container filesystem. Leave blank to use
# $MEDIA_ROOT/Photos; set it only to move the library elsewhere.
IMMICH_UPLOAD_LOCATION=

# API key for the CVS print pipeline to browse the library and fetch originals.
# Create it in Immich: user menu → Account Settings → API Keys → New API Key.
# Read-only usage — the print flow never writes to Immich.
IMMICH_API_KEY=

# Pin to a specific Immich release (e.g. v3.1.0) instead of tracking `release`.
# Worth pinning once the library matters: Immich ships breaking schema
# migrations and a surprise major upgrade on restart is not a good afternoon.
IMMICH_VERSION=

# Zip code of the CVS that collects photo prints (Hawaii). Only used to
# pre-select the pickup store during CVS checkout; leaving it blank just means
# picking the store by hand. See
# DOCS/features/google-photos-cvs-print-automation-spec.md.
CVS_PICKUP_ZIP=

# NOTE: there is no CVS password to store. CVS signs in with a passkey or an
# emailed one-time code, so a human has to do it.
#
# Sign in with your everyday Chrome, export the cvs.com cookies, and run
# scripts/cvs-session-import.sh — it converts them to the session file the
# server carries and pushes it to the NAS state mount.
#
# Do not try to automate the sign-in itself. An earlier script opened
# Playwright's own Chromium for you to sign in inside; CVS fingerprints that
# browser and refuses the login page outright, so the session could never be
# captured. See "Photo prints: CVS blocks the login page in an automated
# browser" in DOCS/ASSERTIONS_AND_ASSUMPTIONS.md.
